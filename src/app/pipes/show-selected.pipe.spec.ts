import { ShowSelectedPipe } from './show-selected.pipe';

describe('ShowSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
